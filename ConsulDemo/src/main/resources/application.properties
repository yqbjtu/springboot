
server.port=8080
spring.application.name=test1
consul.ip=127.0.0.1
consul.port=8500

spring.cloud.consul.host=127.0.0.1
spring.cloud.consul.port=8500
spring.cloud.consul.discovery.service-name=sub-service
spring.cloud.consul.discovery.health-check-interval=120s
spring.cloud.consul.discovery.tags=trial
spring.cloud.consul.discovery.health-check-path=${server.contextPath}/health
spring.cloud.consul.discovery.instance-id=${spring.application.name}:${server.port}:${random.int}